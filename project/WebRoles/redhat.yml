--- # Master Nagios
- hosts: app
  user: root
  sudo: yes
  connection: ssh
  pre_tasks:
   - name: Time to start
     raw: date > /tmp/startime.log
  roles:
    - redhat_web
  post_tasks:
   - name: Time to End
     raw: date > /tmp/endtime.log

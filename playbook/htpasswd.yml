--- # HTPASSWD
- hosts: app
  user: root
  sudo: yes
  connection: ssh
  gather_facts: no
  tasks: 
   - name: Install the python dependencies
     yum: pkg=python-passlib state=latest
   - name: Adding a user to web site authentication
     htpasswd: path=/var/www/html/.htpasswd name=ak state=absent 
